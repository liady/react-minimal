<rt-require dependency="./Item" as="Item"/>
<div>
    <div rt-repeat="item in this.getItems()" key={item.key}>
        <Item text={item.text} done={item.done} onToggle="()=>this.handleToggle(item.text)"/>
    </div>
</div>